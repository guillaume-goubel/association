<div class="card card-activity-message col-12 col-md-10 mb-3">
    
    {# <div class="d-flex justify-content-between">
            
        <div class="feature-box-icon" style="background-color:{{activity.color}};height:55px;width:55px;">
            <div>
                {% if activity.pictureType == "image" %}
                    <img class="my-2" style="width: 30px;" src="{{ customAsset('app/' ~ activity.picture )}}" alt="">
                {% else %}
                    <i class="{{ activity.picture }} align-middle icon-medium text-white my-3"></i>
                {% endif %}
            </div>
        </div>
            
    </div> #}

    <div class="card-body">
        <h5 class="card-title border-bottom">{{ activityMessage.name|capitalize }}</h5>

        <div class="row">
            <p class="card-text preformatted fs-14">{{activityMessage.description|raw}}</p>
        </div>

        <div class="d-flex justify-content-end">
            <a href="{{ path('admin_activity_message_edit', { 'id':activityMessage.id }) }}" class="edit-administrator-btn btn btn-very-small d-table d-lg-inline-block lg-mb-15px mx-2">
                <span>
                    <i class="fa-solid fa-pen-to-square"></i>
                </span>
            </a>
        </div>

    </div>

</div>